{
  "name": "quiet-silence-dd82",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "start": "wrangler dev",
    "test": "vitest",
    "kv": "wrangler kv:namespace create"
  },
  "devDependencies": {
    "wrangler": "^3.0.0",
    "vitest": "1.3.0",
    "@cloudflare/vitest-pool-workers": "^0.1.0"
  }
}